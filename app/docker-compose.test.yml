services:
  test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pop-test
    working_dir: /app
    command: sh -c "npm ci && npm test"
    networks:
      - pop-test-net

networks:
  pop-test-net:
    driver: bridge
