services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pop-app
    ports:
      # Puerto asignado por registry: 3006 → interno 8080 (Node.js server)
      # URL pública: https://pop.qampuapp.com
      - "${PORT:-3006}:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - pop-net

  # ── Descomentar según necesidad ────────────────────────────────────────────
  # db:
  #   image: postgres:16-alpine
  #   container_name: pop-db
  #   environment:
  #     POSTGRES_DB: ${POSTGRES_DB:-pop}
  #     POSTGRES_USER: ${POSTGRES_USER:-admin}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
  #   volumes:
  #     - db-data:/var/lib/postgresql/data
  #   networks:
  #     - pop-net

  # redis:
  #   image: redis:7-alpine
  #   container_name: pop-redis
  #   networks:
  #     - pop-net

networks:
  pop-net:
    driver: bridge

# volumes:
#   db-data:
