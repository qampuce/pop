{
  "version": "0.5.0",
  "basePath": "/",
  "description": "pop — orquestador de pagos multi-provider con routing inteligente, cascading, webhooks normalizados y store in-memory de operaciones",
  "endpoints": [
    {
      "method": "GET",
      "path": "/health",
      "description": "Health check del servicio con versión y uptime"
    },
    {
      "method": "GET",
      "path": "/providers",
      "description": "Lista los providers de pago registrados en el orquestador"
    },
    {
      "method": "POST",
      "path": "/api/v1/tokenize",
      "description": "Tokeniza datos de pago (card/APM) via el provider indicado"
    },
    {
      "method": "POST",
      "path": "/api/v1/charge",
      "description": "Ejecuta autorización + captura en una operación con routing y cascading"
    },
    {
      "method": "POST",
      "path": "/api/v1/authorize",
      "description": "Reserva fondos sin capturar (auth-only) con routing y cascading"
    },
    {
      "method": "POST",
      "path": "/api/v1/capture",
      "description": "Captura fondos de una autorización previa contra el provider original"
    },
    {
      "method": "POST",
      "path": "/api/v1/refund",
      "description": "Devuelve fondos total o parcialmente de un pago capturado"
    },
    {
      "method": "POST",
      "path": "/api/v1/void",
      "description": "Cancela una autorización pendiente antes de su captura"
    },
    {
      "method": "GET",
      "path": "/api/v1/payments",
      "description": "Lista pagos con filtros (tenant_id, status, provider, reference, limit)"
    },
    {
      "method": "GET",
      "path": "/api/v1/payments/{id}",
      "description": "Obtiene un pago específico por ID"
    },
    {
      "method": "GET",
      "path": "/api/v1/refunds",
      "description": "Lista refunds con filtros (tenant_id, status, provider, payment_id, limit)"
    },
    {
      "method": "GET",
      "path": "/api/v1/refunds/{id}",
      "description": "Obtiene un refund específico por ID"
    },
    {
      "method": "GET",
      "path": "/api/v1/metrics",
      "description": "Métricas agregadas de pagos y refunds (totales, por status, por provider, montos)"
    },
    {
      "method": "POST",
      "path": "/webhooks/{provider}",
      "description": "Recibe y normaliza webhooks del proveedor indicado (firma verificada)"
    }
  ],
  "events": [
    "payment.authorized",
    "payment.captured",
    "payment.failed",
    "payment.voided",
    "payment.pending",
    "refund.created",
    "refund.completed",
    "refund.failed",
    "dispute.opened",
    "dispute.resolved"
  ],
  "schemas": [],
  "consumes": []
}
